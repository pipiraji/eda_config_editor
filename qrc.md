
# 📄 QRC 환경 파일 자동화 스크립트 개발 계획서

## 1. 시스템 개요

본 스크립트는 QRC 설정 파일 내부의 복잡한 계층 구조를 인식하고, 사용자가 입력한 명령어(Action)에 따라 설정값을 안전하게 조작(수정, 추가, 삭제, 주석 관리)하는 자동화 툴입니다.

### 1.1 용어 정의

* **Action (실행 명령어):** 스크립트에 지시하는 동작 (`uncomment`, `update`, `set`, `delete`)
* **Command (커맨드 블록):** 설정 파일 내의 상위 섹션 (예: `input_db`)
* **Option (옵션):** 커맨드 하위의 세부 설정 키 (예: `-run_name`)
* **Value (값):** 옵션에 할당되는 실제 데이터 (예: `Design`)

> **CLI 실행 형태:**
> `python qrc_editor.py [파일명] [명령어] --command [커맨드명] --option [옵션명] --value [값]`
> *(예: `python qrc_editor.py qrc.tcl set --command input_db --option run_name --value "Design"`)*
> *(주의: `--option` 인자는 `-` 없이 입력해야 하며, 내부적으로 자동 적용됩니다.)*

---

## 2. 핵심 설계 원칙 및 프리프로세싱 (Preprocessing)
파일을 직접 수정하는 대신, **메모리에 파일 구조를 먼저 로드하여 분석하는 과정**을 거칩니다.

1. **구조화 및 캐싱 (State Mapping):** 파일을 읽어 `커맨드 -> 옵션 -> 값` 형태의 트리(Tree) 또는 딕셔너리(Dictionary) 구조로 메모리에 기억합니다. 주석 처리 여부도 함께 상태값으로 저장합니다.
2. **명령어 체이닝 (Action Chaining):** 기능의 중복 구현을 막기 위해 하위 명령어를 호출하여 재활용합니다. (예: `set` 실행 시 `update` 우선 호출)
3. **상태 동기화 (State Sync):** 체이닝 과정에서 `uncomment`가 실행되어 상태가 변하면, 메모리의 데이터 구조를 즉시 업데이트한 후 다음 단계(`update` 등)를 진행합니다.

---

## 3. 명령어(Action)별 상세 명세 및 보완 로직

기존에 충돌이 발생할 수 있었던 "정규표현식 매칭"과 **"삭제/주석의 혼용"** 문제를 보완하여 명세합니다.

### ① uncomment (주석 해제)

* **매칭 대상:** `--command`, `--option`, `--value` 모두 정규표현식(Regex)을 지원하며, 입력된 모든 조건의 **AND 조건**으로 대상을 찾습니다.
* **동작 로직:**
1. 정규표현식 조건에 부합하는 모든 대상을 찾습니다.
2. 해당 라인이 주석 처리(`#`)되어 있다면 주석 기호를 제거하여 활성화합니다.
3. 커맨드 하위의 옵션이 활성화될 경우, 상위 커맨드 라인도 자동으로 활성화됩니다.

### ② update (수정 전용)

* **선행 작업:** 내부적으로 `uncomment` 로직을 먼저 실행하여 대상을 활성화합니다.
* **매칭 대상:** `--command`와 `--option`은 정규표현식을 지원하며, 두 조건의 **AND 조건**으로 대상을 찾습니다.
* **동작 로직:** 매칭된 커맨드/옵션의 값(Value)을 입력받은 새로운 값으로 덮어씁니다. 대상이 없으면 작업을 수행하지 않습니다.
* **보완점 (값 literal 처리):** 업데이트할 `--value` 인자에는 정규표현식 동작을 허용하지 않으며, 입력된 문자열 그대로를 사용합니다.

### ③ set (설정 또는 추가)

* **선행 작업:** 내부적으로 `update` 로직을 먼저 실행합니다.
* **동작 로직:**
1. `update`가 성공적으로 하나 이상의 값을 수정했다면 그대로 종료합니다.
2. 매칭되는 대상이 없어 `update`가 무시되었다면, 해당 커맨드 블록의 가장 마지막에 `옵션`과 `값`을 새로 추가합니다.
* **매칭 대상 (정규식 불가):** `--command`와 `--option`에 정규표현식을 허용하지 않습니다. 입력된 문자열과 정확히 일치하는 대상을 찾거나 생성합니다.
* **보완점:** `--value`는 입력된 문자열 그대로(literal) 사용됩니다.

### ④ delete (삭제 - Soft Delete)

* **매칭 대상:** `--command`, `--option`, `--value` 모두 정규표현식을 지원하며, 입력된 모든 조건의 **AND 조건**으로 대상을 찾습니다.
* **동작 로직:**
1. 매칭된 커맨드 하위의 특정 옵션 라인을 **주석 처리(`#`)** 합니다.
2. 만약 옵션 인자 없이 커맨드만 제시되었다면, 해당 커맨드에 속한 모든 옵션을 주석 처리합니다.
* **보완점 (Cascade Commenting):** 옵션을 주석 처리한 후, 해당 커맨드 블록 내에 활성화된 옵션이 단 하나도 남지 않게 된다면, 상위 커맨드 라인 자체도 자동으로 주석 처리합니다.

---

## 4. 시스템 안정성 확보 방안 (Safety Guards)

1. **Dry-run (시뮬레이션) 모드 지원 제안:**
* 정규표현식을 지원하기 때문에, 사용자의 실수로 파일 전체가 변경되거나 주석 처리될 위험이 높습니다. `--dry-run` 옵션을 추가하여, 실제로 파일을 덮어쓰기 전에 **어떤 라인이 어떻게 변경될지 미리 화면에 출력**하는 기능이 필수적입니다.

2. **문법 연결 기호(`\`) 관리:**
* 명령어 추가(`set`) 또는 주석 처리(`delete`) 시, 이전 라인과 다음 라인을 이어주는 역슬래시(`\`) 문법이 깨지지 않도록 프리프로세싱 단계에서 `\`의 위치를 추적하고 재배치하는 로직을 반드시 포함해야 합니다.