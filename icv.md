# 📄 IC Validator #define 자동화 스크립트 개발 계획서 (icv_editor)

## 1. 시스템 개요 및 구조 매핑

본 스크립트는 Synopsys IC Validator (ICV)의 PXL 런셋 설정 파일 내의 `#define` 지시문 선언부를 파싱하여, 사용자의 의도에 맞게 상수 정의 및 활성화 여부를 제어합니다.

### 1.1 구조 매핑 (PXL/C++ 기준)

* **포맷 특징:** `[indent] #define [변수명] [값]` (3항 구조) 또는 `[indent] #define [변수명]` (2항 구조)
* **Variable (변수명):** 제어할 대상 매크로 상수명 (예: `RUN_DRC_CORE`, `GRID_RESOLUTION`)
* **Value (값):** 상수에 할당될 치수 또는 부동소수점 데이터 (예: `0.001`, `0.028`)
* **주석 기호:** 라인 시작 부분의 `//` (예: `// #define RUN_LVS_CHECK`)

> **CLI 실행 형태:**
> `python icv_editor.py [파일명] [명령어] --variable [변수명] --value [값]`
> *(예: `python icv_editor.py icv.pxl set --variable NEW_SWITCH`)*
> *주: --value를 지정하지 않으면 2항 구조(값 없음)로 생성/수정됩니다.*

---

## 2. 핵심 제약 조건 및 스마트 가드 (Smart Guards)

`icv_editor.py`는 기존 단순 치환형 스크립트들과 달리, EDA 실무 런셋 파일의 복잡한 문법 구조를 안전하게 보전하기 위해 아래 3대 세이프티 가드가 설계 및 적용되어 있습니다.

### ① 라인 주석(`//`)만 주석 관리 대상으로 한정
* `#define`을 비활성화할 때 블록 주석(`/* ... */`) 내부에 선언된 것은 무시하고, 오로지 라인 단위의 주석 기호(`//`)만을 관리하고 식별합니다.

### ② 조건부 컴파일 블록(`#ifdef`, `#ifndef`, `#if`, `#endif`) 내부 제외
* `#ifdef` 또는 `#ifndef` 등으로 둘러싸여 그룹으로 묶여 있는 로컬 상수들은 변경 시 전체 컴파일에 부작용을 초래할 수 있으므로, **오직 최상위(Top-level)에 정의된 #define만 식별 및 편집**합니다.

### ③ 완벽한 Spacing 및 주석 정렬(Alignment) 보존
* 런셋 소스 내의 가독성을 위해 맞춰진 각 `#define` 문과 우측 후속 주석(`// 제조 그리드 크기...`) 사이의 **공백 간격(Spacing)을 100% 동일하게 기억하고 재배치**하여 파일 포맷을 훼손하지 않습니다.

---

## 3. 명령어(Action)별 동작 명세

### ① uncomment (주석 해제)

* **매칭 대상:** `--variable` 및 `--value` 모두 정규표현식(Regex)을 지원하며, 입력된 모든 조건의 **AND 조건**으로 대상을 찾습니다.
* **동작 로직:**
  1. 조건에 맞는 최상위 `#define [변수명]`을 포함한 라인을 찾습니다.
  2. 해당 라인 앞의 주석 기호(`//`)를 안전하게 제거하여 상수를 활성화합니다.

### ② update (수정 전용)

* **선행 작업:** `uncomment` 로직 실행.
* **매칭 대상:** `--variable`에 정규표현식을 지원합니다.
* **동작 로직:**
  1. 활성화된 라인 중 조건에 맞는 `#define [변수명]`을 찾습니다.
  2. 변수명 뒤에 오는 **기존 값을 새로운 값으로 완전히 덮어씁니다.** 
  3. `--value`를 누락하면 기존 값을 제거하여 2항 구조(`#define [변수명]`)로 변환합니다.
* **보완점 (값 literal 처리):** 업데이트할 `--value` 인자에는 정규표현식 동작을 허용하지 않으며, 입력된 문자열 그대로를 사용합니다.

### ③ set (설정 또는 추가)

* **선행 작업:** `update` 로직 실행.
* **동작 로직:**
  1. `update`가 성공했다면 그대로 종료합니다.
  2. 대상 변수가 파일에 존재하지 않는다면, 파일의 **맨 마지막 줄**에 `#define [변수명] [값]` 또는 `#define [변수명]` 포맷으로 새 라인을 추가합니다.
* **매칭 대상 (정규식 불가):** `--variable`에 정규표현식을 허용하지 않습니다. 입력된 문자열과 정확히 일치하는 대상을 찾거나 생성합니다.
* **보완점:** `--value`는 입력된 문자열 그대로(literal) 사용됩니다. --value를 지정하지 않으면 2항 구조로 추가합니다.

### ④ delete (삭제 - Soft Delete)

* **매칭 대상:** `--variable`과 `--value` 모두 정규표현식을 지원하며, 입력된 모든 조건의 **AND 조건**으로 대상을 찾습니다.
* **동작 로직:**
  1. 조건에 맞는 `#define [변수명]` 라인 맨 앞에 `// `를 붙여 **주석 처리(비활성화)** 합니다.
  2. 이미 주석 처리된 라인은 중복해서 주석 기호를 붙이지 않고 안전하게 건너뜁니다.
