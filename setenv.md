
# 📄 tcsh 환경 변수(setenv) 자동화 스크립트 개발 계획서 (setenv_editor)

## 1. 시스템 개요 및 구조 매핑

본 스크립트는 `tcsh` 쉘 스크립트 내의 `setenv` 환경 변수 선언부를 파싱하여, 사용자의 의도에 맞게 변수값을 제어합니다.

### 1.1 구조 매핑 (tcsh 기준)

* **포맷 특징:** `[주석] setenv [변수명] [값]` (3항 구조) 또는 `[주석] setenv [변수명]` (2항 구조)
* **Variable (변수명):** 제어할 대상 환경 변수 (예: `VARIABLE_NAME`, `PATH`)
* **Value (값):** 변수에 할당될 데이터 (예: `"value"`, `/usr/local/bin:$PATH`)
* **주석 기호:** 라인 시작 부분의 `#` (다중 `#` 포함, 예: `###setenv`)

> **CLI 실행 형태:**
> `python setenv_editor.py [파일명] [명령어] --variable [변수명] --value [값]`
> *(예: `python setenv_editor.py env.csh set --variable PATH --value "/new/path:$PATH"`)*
> *주: --value를 지정하지 않으면 2항 구조(값 없음)로 생성/수정됩니다.*

---

## 2. 명령어(Action)별 동작 명세

프리프로세싱 단계에서 파일을 읽어 `변수명 -> 값`의 상태를 메모리에 기억(캐싱)하고, 명령어 체이닝(`set` -> `update` -> `uncomment`)을 수행하는 기본 원칙은 동일합니다.

### ① uncomment (주석 해제)

* **매칭 대상:** `--variable` 및 `--value` 모두 정규표현식(Regex)을 지원하며, 입력된 모든 조건의 **AND 조건**으로 대상을 찾습니다.
* **동작 로직:**
1. 조건에 맞는 `setenv [변수명]`을 포함한 라인을 찾습니다.
2. 해당 라인 앞의 **모든 주석 기호(`#`)를 일괄 제거**하여 활성화합니다.



### ② update (수정 전용)

* **선행 작업:** `uncomment` 로직 실행.
* **매칭 대상:** `--variable`에 정규표현식을 지원합니다.
* **동작 로직:**
1. 활성화된 라인 중 조건에 맞는 `setenv [변수명]`을 찾습니다.
2. 변수명 뒤에 오는 **기존 값을 새로운 값으로 완전히 덮어씁니다.**
* **보완점 (값 literal 처리):** 업데이트할 `--value` 인자에는 정규표현식 동작을 허용하지 않으며, 입력된 문자열 그대로를 사용합니다. --value를 누락하면 기존 값을 제거하여 2항 구조(`setenv [변수명]`)로 변환합니다.

### ③ set (설정 또는 추가)

* **선행 작업:** `update` 로직 실행.
* **동작 로직:**
1. `update`가 성공했다면 그대로 종료합니다.
2. 대상 변수가 파일에 존재하지 않는다면, 파일의 **맨 마지막 줄**에 `setenv [변수명] [값]` 또는 `setenv [변수명]` 포맷으로 새 라인을 추가합니다.
* **매칭 대상 (정규식 불가):** `--variable`에 정규표현식을 허용하지 않습니다. 입력된 문자열과 정확히 일치하는 대상을 찾거나 생성합니다.
* **보완점:** `--value`는 입력된 문자열 그대로(literal) 사용됩니다. --value를 지정하지 않으면 2항 구조로 추가합니다.



### ④ delete (삭제 - Soft Delete)

* **매칭 대상:** `--variable`과 `--value` 모두 정규표현식을 지원하며, 입력된 모든 조건의 **AND 조건**으로 대상을 찾습니다.
* **동작 로직:**
1. 조건에 맞는 `setenv [변수명]` 라인 맨 앞에 `# `를 붙여 **주석 처리(비활성화)** 합니다.
2. 이미 주석 처리된 라인(`###setenv` 등)은 중복해서 `#`를 붙이지 않고 건너뜁니다.



---

## 3. tcsh 특화 예외 처리 및 안정성 확보 (Safety Guards)

1. **따옴표(`""`, `''`) 및 띄어쓰기 보존 로직 (Critical):**
* `setenv`의 값은 내부에 띄어쓰기가 있을 경우 따옴표로 묶이는 경우가 많습니다. (예: `setenv VAR "A B C"`)
* **해결책:** 파싱 시 `setenv [변수명]` 이후부터 라인 끝까지를 통째로 '값(Value)'으로 취급합니다. 사용자가 CLI에서 값을 입력할 때 따옴표를 포함해 전달하면, 스크립트는 이를 훼손하지 않고 그대로 파일에 기록해야 합니다.


2. **환경 변수 확장(`$PATH` 등) 문자열 보호:**
* 값 내부에 `$` 기호가 포함된 경우(예: `/usr/local/bin:$PATH`), 파이썬 스크립트 내부나 쉘에서 의도치 않게 치환(Expansion)되지 않도록 문자열(Literal) 처리의 우선순위를 높입니다.


3. **다중 주석(`###`) 유연성:**
* 사용자가 임시로 여러 개의 `#`를 붙인 경우를 대비해, 정규표현식 파싱 시 `^#+` 형태로 매칭하여 주석 개수에 상관없이 정확히 `setenv` 블록을 식별하도록 구성합니다.


4. **우측 설명 주석 및 정렬 공백 보존 (Spacing Preservation):**
* 변수 선언 뒤쪽에 붙은 우측 후속 설명 주석(예: `# 패스 지정`)과 변수값 사이의 **정렬용 공백 간격을 정밀하게 기해하고 보존**하여 파일의 원래 포맷 스타일을 훼손하지 않습니다.

